{% extends 'base.html.twig' %}

{% block title %}Réparations {% endblock %}

{% block body %}
    <style>
        .hero-image-small {
            height: 35vh;
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
            position: relative;
            z-index: -99;
            margin-bottom: 50px;
        }
    </style>
    <link rel="stylesheet" href="{{ asset("css/clesMinute.css") }}"  >

    <div style=" background-position-y: -40px; height: 30vh; " id="hero-image-navbar-trigger" class="hero-image-small reparationOrdinateurCover mb-5"> </div>

{#    <div id="hero-image-navbar-trigger" style="height: 5vh;margin-bottom: 10vh"></div>#}

    <div class="container">

       <div class="row mb-5">
            {% for marque in marquesDeCategorie %}
        <div class="col-3 col-md-4">
            <a href="{{ path('list-produits-marque-categorie', {'id_marque' : marque.id, 'id_categorie' : categorie.id}) }}">
                <div  class="smoothCard">
                    <div class="imageCover_brand" style="width: 100%;height: 200px">
                        <img style="object-fit: cover; max-height: 100%; max-width: 100%" src="{{ asset("images/brand_images/" ~ marque.image ) }}" alt="{{ marque.image }}">
                    </div>
                </div>
            </a>
        </div>
            {% endfor %}
        </div>
   </div>
{% endblock %}
